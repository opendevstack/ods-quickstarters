# TODO: map credentials here as they are stored prefixed with project_id?

credentials:
  azureSubscriptionId: @project_id@-cd-subscription-id
  azureTenantId: @project_id@-cd-tenant-id
  azureClientId: @project_id@-cd-client-id
  azureClientSecret: @project_id@-cd-client-secret
